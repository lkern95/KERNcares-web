name: CI/CD

on: 
  push:
    branches:
      - master
        runs-on: ubuntu-latest 
        steps:
          - uses: action/checkoput@v2
            with:
              node-version: 12
          - run: npm ci
          - run: npm test
          - run: npm run build
